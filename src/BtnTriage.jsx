import './BtnTriage.scss';
import SortIcon from '@material-ui/icons/Sort';

export default function BtnTriage (props) {
    return (
        <button className="BtnTriage" >
            <SortIcon />
        </button>
    )
}